{
    "scripts": {
        "api": "node api/app.js",
        "web": "cd web && ng serve",
        "start": "concurrently \"npm run api\" \"npm run web\""
    },
    "dependencies": {
        "concurrently": "^8.2.0"
    }
}
