{
    "scripts": {
        "api": "node api/app.js",
        "web": "cd web && ng serve",
        "install": "concurrently \"cd api && npm install\" \"cd web && npm install\"",
        "start": "concurrently \"npm run api\" \"npm run web\""
    },
    "dependencies": {
        "concurrently": "^8.2.0"
    }
}
